# Sunny
A personal project to learn about emulation and low-level programming

## Build
Sunny is currently only supported on Windows, Use the batch file to build the project.

Note that a valid BIOS file is required to run the application.

## TODO
While many components require additional work/completion, Most of them are implemented, and
Sunny is able to boot into a few games.

The Vulkan renderer is currently deprecated, and hardware rendering will be supported in the future.
